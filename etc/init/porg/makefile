install:
	cd ~/.build; \
	wget http://downloads.sourceforge.net/project/porg/porg-0.8.tar.gz; \
	tar xvf porg-0.8.tar.gz; \
	cd porg-0.8; \
	./configure --disable-grop; \
	make; \
	sudo make install; \
	sudo make logme
